ls -a
ls --all #is the same
ls -a / #is used to list all files and directories in the root directory of the system (/), including hidden ones.
ls -l -a -h
ls -l -ah
ls -lah #All three do the same thing, but it's a way to shorten the command.